// Import the actual StudentWriterList component
import StudentWriterList from '../src/screens/StudentWriterList';

// Use the real StudentWriterList component instead of mock data
export default StudentWriterList;